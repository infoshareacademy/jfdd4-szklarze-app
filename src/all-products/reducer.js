import { INCREASE_AMOUNT, DECREASE_AMOUNT } from './actionTypes'

const initialState = {
    itemsToBuy: []
}

export default (state = initialState, action) => {
    switch  (action.type) {
        default:
            return state
    }
}