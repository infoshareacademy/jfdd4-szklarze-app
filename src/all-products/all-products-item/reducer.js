import { GET_AMOUNT } from './actionTypes'

const initialState = {
    currentCounterValue: 55
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}